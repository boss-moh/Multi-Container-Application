version: "3.8"

services:
  app:
    build: .
    container_name: node_app_prod
    ports:
      - "3000:3000"
    environment:
      - PORT=${PORT}
      - MONGO_URL=${MONGO_URL}
    env_file:
      - .env
    restart: always
